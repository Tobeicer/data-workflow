# 1688 来源适配器

状态：`stabilizing`。正式入口已建立；现有 `src/` 与 `tests/` 保持不变，其余实现/资产迁移待后续任务完成。

迁移完成前继续使用当前指南：`data-workflow/1688/1688_公开商品采集流程.md`。现有代码支持商品/SKU、公司解析和单商品浏览器试采，但本目录当前不宣称已有可启用的 n8n 工作流。

## 下一步稳定化门槛

- 建立按店铺 URL 或 `memberId` 去重的多公司任务队列；
- 统一商品、SKU 和公司阶段的批次、断点与 `run_result.json`；
- 提供统一 `run_source.py`、`--dry-run`、有限重试和人工登录状态；
- 验证质量门禁、停止条件和 n8n 状态路由；
- 连续稳定运行验收后再申请启用 n8n。
