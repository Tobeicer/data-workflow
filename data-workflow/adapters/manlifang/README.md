# 漫立方来源适配器

状态：`stabilizing`。正式入口已建立；实现/资产迁移待后续任务完成。

迁移完成前继续使用当前指南：`data-workflow/manlifang/漫立方抓包流程.md`。该指南记录现有全量采集、清洗、图片和交付命令；本目录当前不宣称已有统一 CLI 或可启用的 n8n 工作流。

## 下一步稳定化门槛

- 将当前工具迁入统一 `run_source.py` 与 `--dry-run` 契约；
- 生成符合统一 Schema 的 `run_result.json`；
- 验证新增、修改、下架、无变化和失败恢复；
- 保持原始响应、结构化 JSONL、哈希原图和现有硬链接拓扑可核对；
- 通过质量门禁和连续稳定运行验收后再申请启用 n8n。
