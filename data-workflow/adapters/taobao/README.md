# 淘宝来源适配器

状态：`prototype`。正式入口已建立；实现/资产迁移待后续任务完成。

迁移完成前继续使用当前指南：`data-workflow/taobao/淘宝公开商品采集验证.md`。现有能力是人工授权、低频搜索和详情参数补采原型；本目录当前不宣称已有统一 CLI 或可启用的 n8n 工作流。

## 下一步稳定化门槛

- 将原型迁入统一 `run_source.py`、阶段契约和 `--dry-run`；
- 保存可重放 L0、标准化 L1、质量与变化 L2；
- 输出符合统一 Schema 的 `run_result.json`；
- 对登录失效、验证码、页面结构变化和空解析返回明确状态；
- 通过小样本、质量门禁和连续稳定运行验收后再申请启用 n8n。
