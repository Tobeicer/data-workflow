# 漫立方数据接入

状态：通过邀请入驻获取数据 API，同步全量商品。已交付 3128 条商品、5528 张图片。

本文件在大型资产物理迁移前只记录当前资产位置和交付事实。正式采集代码、复采命令、停止条件和后续门禁以 `../adapters/manlifang/README.md` 为准。

## 1. 当前成果

- 交付批次：`captures/manlifang_full_20260710_110814/`
- 唯一商品：3128 条
- 规范化交付图片：5528 张
- 原始 XLSX：`captures/manlifang_full_20260710_110814/漫立方_原始全量商品数据_manlifang_full_20260710_110814.xlsx`
- 清洗 XLSX：`captures/manlifang_full_20260710_110814/cleaned/漫立方_新全量清洗主数据_20260712.xlsx`
- 最终交付：`漫立方_全量数据/`

清洗以结构化 JSONL、原始响应和哈希原图为源。

## 2. 数据范围

通过 API 同步全量商品资料：分类、商品、详情、SKU、规格、价格、库存、图片和来源标识。

## 3. 处理工具

| 文件 | 用途 |
|---|---|
| `tools/clean_manlifang_full.py` | 生成清洗、类目、图片映射和复核队列 |
| `tools/build_manlifang_delivery_package.py` | 生成 L3 XLSX 和规范化图片交付包 |

## 4. 数据规则

- 批次内以来源商品 ID 追溯，跨批次以 `product_code` 作为稳定业务键。
- 保存商品与类目的多对多关系，不把单一分类覆盖成最终事实。
- `real_category` 和 `all_real_categories` 保存真实来源分类。
- `v2_category_candidate` 用于当前 L3 兼容映射，空值不代表来源分类缺失。
- 原图按 SHA-256 去重；逻辑图片名为 `MLF_<product_code>_<role>_<sequence>_<sha8>.<ext>`。
- `manufacturer_name=漫立方` 是来源方标签，不等同于已核验工商主体。
- 没有真实公开商品 URL 时，`source_public_url` 留空，不构造伪链接。
- 价格、库存、状态和图片变化进入带时间的 L2 快照或变化集。

图片角色：`main`、`gallery`、`detail`、`parameter`、`sku`、`unknown`。

## 5. 增量同步

定期调用 API 获取数据，与上一批次比较：

- 新增和下架商品；
- 价格、库存、图片和分类变化；
- API 字段结构变化；
- 无变化结果。

变化结果进入增量处理；L0 新批次独立保存，不覆盖上一批原始资产。

## 6. 交付边界

原始响应、结构化 JSONL 和哈希原图属于 L0；清洗、关系、快照和质量属于 L1-L2；最终 XLSX 和规范化图片属于 L3。

数据岗位不创建或维护正式数据库表，不直接导入 `public.product`、`public.accessory` 或 `public.manufacturer`。平台负责校验、审核、晋级和发布。

## 7. 验收

- 全量商品数据接口响应完整。
- 商品覆盖率不低于 99%，失败记录可追溯。
- 多规格商品能还原规格组、有效 SKU、价格、库存和图片关系。
- 图片引用均可访问或具备明确失败状态。
- Excel、图片、结构化记录数量可互相核对。
