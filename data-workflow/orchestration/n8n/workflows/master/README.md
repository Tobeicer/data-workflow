# n8n 主工作流

本目录是未来总入口的位置，当前不包含可启用的工作流 JSON。

总入口只负责：

1. 生成唯一 `run_id`；
2. 获取来源锁，避免同来源冲突运行；
3. 调用已启用的来源子工作流；
4. 读取来源适配器生成的 `run_result.json`；
5. 按成功、人工复核、可重试失败或永久停止路由状态。

采集、解析、清洗、图片处理、质量计算和交付生成不写入主工作流代码节点。
