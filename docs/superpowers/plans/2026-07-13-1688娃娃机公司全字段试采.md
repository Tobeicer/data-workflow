# 1688 娃娃机公司全字段试采实施计划

日期：2026-07-13  
状态：单商品浏览器批次已完成并通过线上验证

## 目标

把已验证的单商品公司信息采集整理为可测试、可重放、可接入统一工作流的 L0-L2 适配器。

## 已完成

- [x] 使用“商用娃娃机”定位 `offer_id=994122564753`。
- [x] 保存商品、店铺、公司档案、联系方式和相关响应证据。
- [x] 验证 1688 官方主体资质页及结构化 `businessInfo`。
- [x] 创建 `data-workflow/adapters/1688/src/company_profile.py`。
- [x] 创建固定样本与 `test_company_profile.py`。
- [x] 创建 `data-workflow/contracts/schemas/1688_company_asset.schema.json`。
- [x] 解析测试覆盖公司、联系方式、工厂双快照、证书/专利口径和字段证据。

验证：

```powershell
python -m pytest data-workflow/adapters/1688/tests/test_company_profile.py -v
```

## Task 1：浏览器公司试采器

文件：

- 新建 `data-workflow/adapters/1688/src/collect_company_pilot.py`
- 新建 `data-workflow/adapters/1688/tests/test_collect_company_pilot.py`

要求：

- [x] 使用假的页面适配器先测试导航顺序、L0 文件名和受限状态。
- [x] 参数包含 `--offer-id`、`--output-dir`、`--delay-seconds`、`--debug`。
- [x] 复用现有人工登录 profile，不把 Cookie 写入输出。
- [x] 保存商品、店铺/工厂和主体资质页面的 HTML、JSON、URL、时间和哈希。
- [x] 登录、验证码、入口缺失返回明确状态；后续扩大批次时继续补强 403/429 和结构漂移分类。

验证：

```powershell
python -m pytest data-workflow/adapters/1688/tests/test_collect_company_pilot.py -v
```

## Task 2：L1/L2 与运行状态

- [x] 输出商品、店铺、公司、工厂、证书、专利和公开联系方式。
- [x] 输出商品—店铺、商品—公司和店铺—公司关系。
- [x] 输出冲突、复核队列和质量报告。
- [x] 生成 `run_manifest.json` 和 `run_result.json`。
- [x] 未知数值保持 null，摘要和明细口径分别保存。

## Task 3：统一入口与验证

文件：

- 修改 `data-workflow/1688/run_1688_workflow.py`
- 修改 `data-workflow/1688/1688_公开商品采集流程.md`

要求：

- [x] 增加 `company` 子命令。
- [x] 统一输出到 `data-workflow/runtime/runs/1688/<run_id>/`。
- [x] `--dry-run` 不访问线上页面。
- [x] 线上验证只运行单商品、低频任务。

线上成功批次：`data-workflow/runtime/runs/1688/1688_claw_machine_pilot_20260713_04/`。结果为 `success`，包含 5 类页面、36 个相关接口响应、64 条字段证据和 25 项公司媒体证据。

完整验证：

```powershell
python -m pytest data-workflow/adapters/1688/tests -v
```

成功标准：全部测试通过；线上任务成功或返回明确受限状态；不得静默输出空成功。
