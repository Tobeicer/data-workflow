# 数据工作流目录迁移收尾实施计划

## 执行约束

- 先关闭复审遗留，再迁移本地资产。
- 资产迁移逐项串行；目标存在、路径越界或 manifest 不一致时停止。
- 不启用 n8n，不运行在线全量采集，不写正式业务表，不创建 Git 提交。

## 任务

1. 删除失效计划，修复治理测试和来源定位冲突。
2. 安全快进主工作区并核验正式目录和 registry。
3. 使用 manifest 验证并同盘移动本地资产。
4. 对齐执行基线、README、adapter、兼容指南和迁移映射。
5. 清理旧路径并执行全量最终验收。
