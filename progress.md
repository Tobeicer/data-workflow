# 当前进度

日期：2026-07-14

- 已确认仓库存在 `.codegraph/`，并使用 CodeGraph 定位漫立方图片清洗和交付生成链路。
- 已确认 `captures` 约 3.423 GiB，`漫立方_全量数据` 约 929.28 MiB。
- 已开始审计，尚未删除任何业务数据或图片。
- 已读取 README、当前执行基线和漫立方来源指南，确认 L0-L3 保留边界。
- 已完成目录和扩展名盘点，锁定 `cleaned/images` 8507 个分类复制图片、约 1.941 GiB 为首要候选。
- 已确认清洗图片优先使用硬链接，正在核对物理占用、工作簿引用和可重建性。
- 已确认 8506 个逻辑图片全部为硬链接且工作簿源路径全部指向 L0，决定保留逻辑路径层以维持 L1-L2 一致性。
- 已读取 1688 与淘宝来源指南，确认保留登录 Profile 和正式 L0 运行证据，只清理可再生缓存及旧调试输出。
- 已确认无采集器/Profile 活动进程；已定位 6 个源码缓存目录、pytest 缓存和 `runtime/tmp/pytest_multi` 测试临时树。
- 删除候选已完成引用、进程、数据层和 Git 忽略验证，准备执行边界校验后清理。
- 首次删除在 pytest 目录符号链接处因 PowerShell 空引用异常中止，其他候选未删除；已改用 .NET 单独删除该工作区内链接。
- 已完成白名单清理：8393 个文件、553.14 MiB 逻辑大小；磁盘可用空间增加 565.32 MiB，无残留目标。
- 全量数据校验首次仅因关系行数预期口径错误退出 1；5527 个唯一 L0 文件的存在性和 SHA-256 已全部通过，已修正预期准备重跑。
- 修正口径后完整数据校验退出 0；最终全套 57 项测试通过，漫立方 L0-L3 关键计数和路径一致性全部通过。
- 用户指出多个大图片目录后已重新核算：`cleaned/images` 是逻辑硬链接层，真正重复占用集中在 L3 的 4415 个普通复制文件（832.96 MiB）。
- 已删除清洗目录唯一未引用的旧占位图片；第二阶段需确认 L3 独立交付保留策略。
- 近期工具兼容问题：`codegraph.ps1` 被执行策略拦截，已改用 `.cmd`；旧版 PowerShell 不支持 `??`，已改用显式空值判断。
- XLSX 首次审计因 PowerShell 原生参数转义失败、硬链接首次抽样因旧版管道语法失败，均已定位并改用兼容写法。
