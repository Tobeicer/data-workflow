# 当前进度

日期：2026-07-13

## 本轮任务

- 已读取项目级迁移规则和相关执行技能。
- 已确认 `.codegraph/` 存在，源目录没有 `.git`。
- CodeGraph 的 `codegraph.ps1` 入口被本机 PowerShell 执行策略拦截；改用同一工具的 `.cmd` 入口继续，不重复该失败方式。
- 已完成代码、文档、IDE、虚拟环境、浏览器日志、批次元数据和符号链接的旧路径审计。
- 已将活跃文本中的旧根目录路径改为 `E:\Desktop_zm\data-workflow`，复扫结果为 0 个残留。
- 已克隆 GitHub 仓库到目标目录，并验证远端、分支和初始内容。
- 首次迁移在移动 `.codegraph` 时收到 Windows `Access denied`，迁移循环在第一个项目即停止，源项目内容尚未移动；目标占位 README 已删除，目标 `.git` 保持完好。
- 已确认目录 ACL 正常；根因是项目专属 CodeGraph 守护进程占用数据库文件，另发现表格预览进程打开交付 XLSX。
- 已只停止这两个明确占用迁移前项目路径的进程；`.codegraph` 单项移动验证通过。
- 其余 15 个顶层项目项全部移动完成，迁移前目录当前为空，新仓库 `.git` 完整保留。
- 已完成迁移后故障复现：虚拟环境 Python 正常，控制台入口失败；测试符号链接为断链。
- 第一次入口修复尝试使用 `python -m venv --upgrade`，命令成功但未重建既有 `pip`/第三方 Windows 启动器，验证仍为退出码 1；不重复该方法。
- 通过可用的 `python -m pip` 精确重装同版本入口所属包后，`pip`、`playwright`、`f2py`、`mitmweb`、`flask`、`normalizer` 均从新路径运行且退出码为 0。
- 首次全目录残留扫描被已知断链触发 `os error 3` 中断，未作为验证结论；先重建链接后再复扫。
- 已重建测试符号链接并验证目标存在。
- 复扫确认控制台入口二进制无旧路径；已删除 4,769 个仅含旧编译路径的 `.pyc` 和 32 个浏览器生成日志，保留漫立方 L0 批次元数据。
- 已在新仓库根目录重建 CodeGraph：索引 32 个代码文件、662 个节点、1,895 条边。
- 迁移前目录内容计数为 0，但删除空目录时 Windows 报“被另一进程使用”；不重复强删，先定位是否为当前 Codex 工作区句柄。
- 已终止一个明确使用旧工作目录的中断遗留 Codex Node 子进程；第二次删除空目录仍报相同占用错误，确认当前桌面任务仍持有目录。停止继续删除，保留 0 文件空目录。
- 最终验证已确认：仅保留 1 个预期 L0 历史路径，二进制残留为 0；Git 远端/分支/忽略规则正确；3128 条商品、5528 张图片和 3 个关键 XLSX 存在；57 个测试通过。
- 脚本烟雾检查首次因入口路径假设和输出截断方式错误而无效；CodeGraph 定位真实入口后重跑，1688 与漫立方帮助命令均退出码 0。
- 最终门禁首次将多个路径同时传给 `git check-ignore --quiet`，Git 返回 128（该模式只接受单一路径）；其他检查通过。改为逐项检查后重跑，不复用错误参数。
- 本次迁移和验证已完成；未执行 Git 提交或推送。
