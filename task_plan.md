# 当前任务计划

## 目标

将游艺圈项目内容迁移到从 `https://github.com/Tobeicer/data-workflow.git` 克隆得到的 `E:\Desktop_zm\data-workflow`，并确保脚本、配置和活跃文档不再依赖迁移前的主目录名。

## 执行阶段

- [x] 审计仓库结构、Git 状态和旧主目录引用
- [x] 仅修正表示文件系统根目录的 `游艺圈` 引用，保留项目品牌与业务语义中的“游艺圈”
- [x] 克隆新仓库并确认目标目录只含新仓库初始内容
- [x] 迁移全部项目内容，保留新仓库 `.git`
- [x] 验证旧路径引用、文件数量、关键资产、脚本/文档一致性和 Git 状态

## 边界与决策

- `docs/project-split/` 和 `docs/requirements/` 为受保护历史参考，不修改其内容；若其中存在旧路径，只在最终报告中说明。
- 不将项目品牌名、数据库对象名或业务描述中的“游艺圈”替换为 `data-workflow`。
- 不执行提交或推送，除非用户另行明确要求。
- 迁移时不覆盖或删除新仓库的 `.git`。
- 漫立方正式批次 `batch_metadata.json` 中的旧 `batch_dir` 作为采集时证据保留；运行流程使用命令行传入的当前批次目录，不依赖该字段。
